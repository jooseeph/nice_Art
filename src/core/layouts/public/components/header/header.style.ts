import {createUseStyles} from 'react-jss';

const styles = {
  header: {
    // position: 'fixed',
    // top: 0,
    // zIndex: 100,
    width: "100%",
  },
  historyBar: {

  },
};

export const useHeaderStyles = createUseStyles(styles);
